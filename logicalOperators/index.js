// Logical operators  = used to combine or manipulate boolean valiues
//                      True or False

//  && ,||, !

const temp = 200;

if (temp >0 && temp<=30){
    console.log("The Weather is Good ");
}
else{
    console.log("The weather is bad");
}